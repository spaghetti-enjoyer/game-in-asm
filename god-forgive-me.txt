cp Playground/raylib/src/libraylib.a VU\ Amsterdam/Computer/Organization/draft

test: test.o
	clang -arch x86_64 -o test test.o -L. -lraylib

test.o: test.S
	clang -arch x86_64 -c -o test.o test.S

#########################

test: test.o
	clang -o test test.o -L. -lraylib

test.o: test.S
	clang -c -o test.o test.S

clean:
	rm -f test test.o